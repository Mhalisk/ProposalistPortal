<%= form_with(model: job, local: true) do |form| %>
  <% if job.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(job.errors.count, "error") %> prohibited this job from being saved:</h2>

      <ul>
      <% job.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :customer_name %>
    <%= form.text_field :customer_name, id: :job_customer_name %>
  </div>

  <div class="field">
    <%= form.label :job_link %>
    <%= form.text_field :job_link, id: :job_job_link %>
  </div>

  <div class="field">
    <%= form.label :company_id %>
    <%= form.select(:company_id,
                    options_for_select([
                                        ['Encor Solar', 1], 
                                        ['Powerhome Solar', 2]
                                        ])) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
